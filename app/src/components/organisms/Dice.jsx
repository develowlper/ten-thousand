import React from 'react';
import PropTypes from 'prop-types';

const Dice = ({}) => {
  return 'Dice';
};

Dice.displayName = 'Dice';

Dice.propTypes = {};

Dice.defaultProps = {};

export default Dice;
