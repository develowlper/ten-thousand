{
  "name": "ten-thousand",
  "version": "0.1.0",
  "description": "a monorepo for the then tousand game",
  "main": "index.js",
  "scripts": {
    "clean": "lerna run clean --parallel && rm -rf node_modules && npm i && lerna bootstrap --hoist",
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --parallel",
    "start": "lerna run start --parallel",
    "version": "lerna version --conventional-commits",
    "test": "lerna run test --parallel",
    "jest": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/develowlper/ten-thousand.git"
  },
  "author": "develowlper",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/develowlper/ten-thousand/issues"
  },
  "homepage": "https://github.com/develowlper/ten-thousand#readme",
  "dependencies": {
    "lerna": "^3.20.2"
  },
  "devDependencies": {
    "prettier": "^2.0.4"
  }
}
